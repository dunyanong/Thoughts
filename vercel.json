{
    "version": 2,
    "builds": [
      {
        "src": "./api",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ],
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
  }
